describe("Keyboard Component Integration Tests", () => {
    it("should get keyboard input from the user for the entity through the IOSystem", () => {
        // NOTE: This will need Puppeteer or CypressIO for testing in the brower page
        expect(true).toBe(false);
    });
});