import "mocha";
import {expect} from "chai";
import { ErrorCode, LogError } from "../src";

describe("Error unit testing", () => {
    it("should print an error log to the console");
});

describe("Engine unit testing", () => {
    it("should run with a print for each tick");
});