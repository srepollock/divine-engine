import { Texture } from "../../../src";

describe("Texture Unit Test", () => {
    // NOTE: Textures cannot be created without WebGL
    // NOTE: Need puppeteer for this
    // let t = new Texture("test", 10, 10);
    // it("should have a name", () => {
    //     expect(t.name).toBe("test");
    // });
    // it("should have a width", () => {
    //     expect(t.width).toBe(10);
    // });
    // it("should have a height", () => {
    //     expect(t.height).toBe(10);
    // });
    it("should pass", () => {
        expect(true).toBeTruthy;
    });
});